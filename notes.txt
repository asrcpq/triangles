fix texman bug

after gc, the inner tex id is cleaned and sorted,
but the compiled model wasn't updated

todo:
- partial update(damage)
- multi window
- reuse command buffer
- reuse data in vertex buffer
